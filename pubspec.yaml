name: zage_flutter
description: Flutter SDK to accept payments with Zage.
version: 0.1.0+1
homepage: https://github.com/zage-inc/ZageFlutter

environment:
  sdk: ">=2.17.6 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_inappwebview: ^5.4.3
  url_launcher: ^6.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
